name: Build and push docker image

on:
  push:

jobs:
  build_and_push:
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repository
        uses: actions/checkout@v4

      - name: Testing the runner
        run: echo "The syntax is now correct and the runner is working!"