name: Build and push docker image

on:
  push:

jobs:
  build_and_push1:
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repository
        uses: actions/checkout@v4

      - name: Testing the runner
        run: echo "The syntax is now correct and the runner is working!"

  build_and_push2:
    runs-on: ubuntu-latest
    needs: build_and_push1
    steps:
      - name: Check out the repository
        uses: actions/checkout@v4

      - name: Testing the runner
        run: echo "The syntax is now correct and the runner is working!"

  build_and_push2-1:
    runs-on: ubuntu-latest
    needs: build_and_push2
    steps:   
      - name: Testing the runner
        run: echo "The syntax is now correct and the runner is working!"


  build_and_push2-2:
    runs-on: ubuntu-latest
    needs: build_and_push2
    steps:
      - name: Check out the repository
        uses: actions/checkout@v4

      - name: Testing the runner
        run: echo "The syntax is now correct and the runner is working!"