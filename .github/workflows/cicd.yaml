name: Build and push docker image

on:
  push:

jobs:
  build_and_push1:
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repository
        uses: actions/checkout@v4

      - name: login to AWS ECR
        uses: action/checkout@v2
        with:
            aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
            aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
            aws-region: ${{ secrets.AWS_REGION }}

      - name: login to ECR
        id: login-ecr
        uses: aws-action/amazon-ecr-login@v2

      - name: Docker build
        run: |
          echo "MONGO_USER=chukwuemelieoguejiofor_db_user" >> .env
          echo "MONGO_PASSWORD=68XZ2P2RRCCNxTR4" >> .env
          echo "MONGO_CLUSTER=cluster0.hqahfia.mongodb.net/" >> .env
          ls -la
          cat .env
          docker build -t emelen_backend:latest .

      - name: Docker push
        run: |
          docker tag emelen_backend:latest 618756493952.dkr.ecr.us-east-2.amazonaws.com/emelen_backend:${GITHUB_RUN_NUMBER}
          docker push docker tag emelen_backend:latest 618756493952.dkr.ecr.us-east-2.amazonaws.com/emelen_backend:${GITHUB_RUN_NUMBER}