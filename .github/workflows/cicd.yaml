name: Build and push docker image

on:
  push:

jobs:
  build_and_push1:
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repository
        uses: actions/checkout@v4

      - name: Docker build
        run: 
          echo "${{ vars.ENV_FILE }}" > env.example
          ls -la
          docker build -t emelen_backend:latest .